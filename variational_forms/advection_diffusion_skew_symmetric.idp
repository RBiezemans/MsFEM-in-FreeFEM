vfname = "adv_diffusion_SkewSym";

macro aComplete(u,v) (adif(u,v) + 0.5*aadv(u,v) - 0.5*aadvAdj(u,v) - 0.5*amass(u,v))// EOM
macro aBasis(u,v) aComplete(u,v)// EOM
macro aref(u,v) (adif(u,v) + aadv(u,v))// EOM

//macro aEffDif(u,v,nu,bx,by) aComplete(u,v,nu,bx,by)// EOM
//bilinear form collecting all terms for the effective diffusion
macro aTest1(u,v) (0.5*aadv(u,v) - 0.5*amass(u,v))// EOM
//bilinear form collecting all terms for the effective advection

useVc=1;
fullVF=1;